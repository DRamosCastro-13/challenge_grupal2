const { createApp } = Vue

const options = {
  data() {
    return {

    
    } // finaliza return
  }, // finaliza data
  created() {
   
  }, //finaliza created

  methods: {
  
  }
}

const app = createApp(options)
app.mount('#app')